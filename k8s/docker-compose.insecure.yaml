version: "3.9"

services:
  webapp:
    image: webapp:secure
    container_name: secure-web-demo

    ports:
      - "3000:3000"
      
    environment:
      DB_HOST: "db.internal.company.com"
      DB_USER: "admin"
      DB_PASSWORD: "P@ssw0rd123!"
      STRIPE_API_KEY: ${STRIPE_API_KEY}
      JWT_SECRET: "mysupersecretkey_dont_tell_anyone"